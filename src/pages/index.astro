---
import Text from '../components/Test.astro';
import Layout from '../layouts/Layout.astro';
import Activity from '../components/Activity.jsx';

function updateClock() {
    const date = new Date();
    const hours = String(date.getHours()).padStart(2, '0');
    const minutes = String(date.getMinutes()).padStart(2, '0');
    const seconds = String(date.getSeconds()).padStart(2, '0');
    const time = `${hours}:${minutes}:${seconds}`;
	// console.log(time);
	return time;
  }
//   setInterval(updateClock, 1000);

const date = new Date();

// const activities = ["Bathroom", "Living Room", "Kitchen + Toilet"]

// let newActivity;

// let i = 0;	
// function moveWork(){

// 	if(i < 3){
// 		if(i=0){
// 	 		newActivity = activities[0]
// 	 		i++;
// 		}else{
// 	 		newActivity = activities[i + 1]
// 	 		i++;
// 		}
// 	}else {
// 	 i = 0;
// 	 newActivity = activities[0];
// 	}
// 	console.log(newActivity);
	
// }

// setInterval(moveWork, 2000);

---
<Layout title="Welcome to Astro.">
	<div class="w-full h-screen bg-cover bg-fixed bg-no-repeat bg-center object-cover" 
		 style="background-image: url('/public/2.jpg');">
		<h1 class="text-black font-bold text-center pt-10">WELCOME TO <br>
			3 ACRE PLACE - HOME SWEET HOME
		</h1>
		<h1 class="text-center">{date}</h1>

	<div class="grid grid-cols-2 pt-10 h-52 ">
		<div class="flex w-full justify-center">
		</div>
		<div class="flex flex-col w-full h-full justify-center">
			<div class="border-solid border-4 border-sky-500 rounded-3xl w-2/4 h-20 flex justify-center my-3">
				<div class="self-center">
				 <Activity client:load/>
				</div>
			</div>

			
		</div>
	</div>
	</div>
</Layout>

